[pytest]
minversion = 8.0
addopts = -ra --strict-config --strict-markers
testpaths =
    oven/tests
python_files = test_*.py
markers =
    slow: long-running tests
    fixture: tests that require binary fixture files
xfail_strict = true
